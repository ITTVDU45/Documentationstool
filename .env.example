# Optional: own Chromium path for server/container environments
PUPPETEER_EXECUTABLE_PATH=

# Optional: public app URL for metadataBase in production
NEXT_PUBLIC_APP_URL=

# Optional: separate presentation-ai app (clone https://github.com/allweonedev/presentation-ai), e.g. http://localhost:3001
NEXT_PUBLIC_PRESENTATION_APP_URL=

# Reserved for later persistence phase
MONGODB_URI=
MONGODB_DB=
